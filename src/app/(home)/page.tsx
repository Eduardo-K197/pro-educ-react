import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'ProEduc Admin',
  description: 'Painel administrativo ProEduc.',
};

export default function Page() {
  return <HomeView />;
}
