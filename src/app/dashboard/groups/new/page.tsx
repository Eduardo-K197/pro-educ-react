import { GroupCreateView } from 'src/sections/group/view';

export const metadata = {
  title: 'Novo Grupo | Pro-Educ Admin',
};

export default function GroupNewPage() {
  return <GroupCreateView />;
}
