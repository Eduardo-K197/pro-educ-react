import { CurriculumView } from 'src/sections/curriculum/view/curriculum-view';

export const metadata = { title: 'Grade Curricular' };

export default function Page() {
  return <CurriculumView />;
}
