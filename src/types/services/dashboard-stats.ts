export interface DashboardStats {
  studentCount: number;
  teacherCount: number;
  courseCount: number;
  classroomCount: number;
  totalPending: number;
  totalOverdue: number;
  totalReceived: number;
}
